# Qlty quality process

Qlty ([qlty.sh](https://qlty.sh)) runs separately from SonarCloud. See [`docs/sonar-quality.md`](sonar-quality.md) for Sonar gates and remediation.

## What Qlty analyzes

Configured plugins in [`.qlty/qlty.toml`](../.qlty/qlty.toml) cover **Ruby** (fastlane, Gemfiles, Podfile), **Kotlin** (Android `MainActivity`), **shell** scripts, **GitHub Actions**, and security scanners. They do **not** analyze Dart/Flutter under `photobooth/lib/` — use SonarCloud for app code quality.

## Open issues triage

**Last verified:** 2026-05-21 (local CLI `qlty 0.618.0`)

| Source | Non-coverage issues | Notes |
|--------|---------------------|--------|
| Local `qlty check --all` | **0** (was 68 before remediation) | Implementation source of truth |
| Qlty Cloud dashboard | Reconcile manually | **Issues** tab, filter out coverage metrics |

### Baseline breakdown (before fix — 2026-05-21)

| Area | Count | Plugins |
|------|-------|---------|
| `photobooth/fastlane/Fastfile` | 41 | rubocop |
| `photobooth/android/.../MainActivity.kt` | 7 | ktlint |
| Gemfiles (2) | 12 | rubocop |
| `photobooth/ios/Podfile` | 3 | rubocop |
| `GeneratedPluginRegistrant.java` | 2 | radarlint-java (excluded in config) |
| Security / workflows | 0 | actionlint, trivy, trufflehog, osv-scanner |

**Dashboard reconciliation:** Compare the Cloud **Issues** list (exclude coverage) to the table above. If counts or rules differ, re-run `qlty check --all` from the repo root and align [`.qlty/qlty.toml`](../.qlty/qlty.toml) with the Cloud project settings.

## Local lint checks (non-coverage)

From the **repository root**:

```bash
qlty check --all --level note
```

Expect: `✔ No issues`.

Optional auto-fix for Ruby style only:

```bash
qlty check --fix --filter rubocop <paths>
```

## CI coverage upload

[`.github/workflows/qlty.yml`](../.github/workflows/qlty.yml) on every push to `main` and on pull requests:

1. `flutter test --coverage` in `photobooth/`
2. Upload `photobooth/coverage/lcov.info` via [`qltysh/qlty-action/coverage@v2`](https://github.com/qltysh/qlty-action)

### GitHub secret (required)

1. In Qlty → your repository → **Settings** → create a **coverage token**.
2. In GitHub → **Settings → Secrets and variables → Actions** → add:
   - Name: `QLTY_COVERAGE_TOKEN`
   - Value: the token from Qlty

The upload step uses:

- `files: photobooth/coverage/lcov.info` (Flutter LCOV, not `target/lcov.info`)
- `format: lcov`
- `add-prefix: photobooth` so paths in the report match the monorepo (`lib/...` under `photobooth/`)

Without `QLTY_COVERAGE_TOKEN`, the **Qlty** workflow fails on upload. The **SonarCloud** workflow is unaffected.

## Local coverage (optional)

```bash
cd photobooth
flutter test --coverage
# Report: photobooth/coverage/lcov.info
```

## Exclusions

### Generated Flutter Android registrant

Excluded in [`.qlty/qlty.toml`](../.qlty/qlty.toml) (same as `sonar.exclusions` in [`photobooth/sonar-project.properties`](../photobooth/sonar-project.properties)):

- `**/GeneratedPluginRegistrant.java`

Do not edit this file; Flutter regenerates it on build.

### Vendored / path dependency packages

Path dependencies under `photobooth/packages/` are not app code:

- `photobooth/packages/camera_android_camerax/`
- `photobooth/packages/camera_native_details/`

Do not remove these unless you intend Qlty to analyze vendored forks.

## Relationship to SonarCloud

| | Qlty | SonarCloud |
|---|------|------------|
| Workflow | [`qlty.yml`](../.github/workflows/qlty.yml) | [`sonarcloud.yml`](../.github/workflows/sonarcloud.yml) |
| Secret | `QLTY_COVERAGE_TOKEN` | `SONAR_TOKEN` |
| Config | `.qlty/qlty.toml` | `photobooth/sonar-project.properties` |
| App code (Dart) | Not analyzed | Primary tool |

Both workflows run tests independently today (duplicate `flutter test` per PR). That is intentional for separation; optimize later with a shared coverage artifact if CI time matters.

## Optional follow-up

Add a `qlty check` job to [`.github/workflows/qlty.yml`](../.github/workflows/qlty.yml) so PRs fail on new lint debt (keep the coverage upload job separate).
