<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is limited to the LAN printer and loopback. API traffic uses HTTPS.
  If your event printer uses a different LAN IP, add a <domain> entry before building.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- AppConstants.kDefaultPrinterHost -->
        <domain includeSubdomains="false">192.168.2.108</domain>
    </domain-config>
</network-security-config>
