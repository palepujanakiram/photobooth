<?xml version="1.0" encoding="utf-8"?>
<!--
  Primary action, matching the Flutter capture screen's blue "Take shot" pill. Disabled state
  is the same blue at low alpha rather than grey, which is what the Flutter button does while
  the camera is still negotiating.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="#3D2196F3" />
            <corners android:radius="14dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#1976D2" />
            <corners android:radius="14dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#2196F3" />
            <corners android:radius="14dp" />
        </shape>
    </item>
</selector>
