<?xml version="1.0" encoding="utf-8"?>
<!-- Back chevron for the native capture screen, matching the Flutter capture bar's
     CupertinoIcons.back. A vector rather than a bitmap so it stays crisp on the TV box's
     1080p panel without shipping four densities. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="28dp"
    android:height="28dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@android:color/white">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M15.41,7.41L14,6l-6,6 6,6 1.41,-1.41L10.83,12z" />
</vector>
