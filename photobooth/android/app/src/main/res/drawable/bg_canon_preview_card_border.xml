<?xml version="1.0" encoding="utf-8"?>
<!--
  Border-only overlay for the viewfinder card. Drawn as the FrameLayout's foreground so the
  hairline sits *above* the SurfaceView — a SurfaceView punches its own hole in the window
  and paints over any background behind it, so a stroke on the background alone would be
  covered by the live frame.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="14dp" />
    <stroke android:width="1dp" android:color="#59FFFFFF" />
</shape>
