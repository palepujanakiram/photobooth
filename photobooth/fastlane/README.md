# Fastlane (Android)

Run from `photobooth/`:

```sh
bundle exec fastlane android generatedbuild
```

Edit distribution settings at the top of [`Fastfile`](Fastfile) (`FIREBASE_TOKEN`, `PLAY_STORE_JSON_KEY_PATH`, etc.).

See [DISTRIBUTION.md](DISTRIBUTION.md).
